# Copy to .env.local for development. For production, use Firebase App Hosting
# environment variables / secrets (see apphosting.yaml).

# https://resend.com/api-keys
RESEND_API_KEY=

# Must be a sender Resend allows: verified domain, or onboarding@resend.dev for tests.
RESEND_FROM_EMAIL=onboarding@resend.dev

# Where RSVP notifications are delivered.
RSVP_NOTIFY_EMAIL=moussadek.lina45@gmail.com

# Optional: CC this address on each RSVP notification.
# RSVP_CC_EMAIL=
